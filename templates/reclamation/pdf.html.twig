<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>SwapCircle Reclamation</title>
    <style>
        @page {
            size: A4;
            margin: 0;
        }
        body {
            font-family: 'Helvetica', 'Arial', sans-serif;
            line-height: 1.5;
            color: #333;
            margin: 0;
            padding: 0;
            background-color: white;
        }
        .page {
            width: 210mm;
            height: 297mm;
            position: relative;
        }
        .header {
            background-color: #1a5276;
            padding: 20px 40px;
            color: white;
            height: 60px;
            position: relative;
        }
        .logo-area {
            position: absolute;
            left: 40px;
            top: 20px;
        }
        .logo-text {
            font-size: 24px;
            font-weight: bold;
            letter-spacing: 1px;
        }
        .document-title {
            position: absolute;
            right: 40px;
            top: 30px;
            font-size: 18px;
            text-transform: uppercase;
        }
        .content {
            padding: 40px;
        }
        .section {
            margin-bottom: 30px;
        }
        h1 {
            color: #1a5276;
            font-size: 24px;
            border-bottom: 2px solid #1a5276;
            padding-bottom: 10px;
            margin-top: 0;
        }
        h3 {
            color: #2980b9;
            font-size: 18px;
            background-color: #f0f7fc;
            padding: 8px 15px;
            margin-bottom: 15px;
            border-left: 4px solid #2980b9;
        }
        .info-row {
            margin-bottom: 10px;
            padding: 8px 15px;
            background-color: #f9f9f9;
            border-radius: 4px;
        }
        .info-row:nth-child(even) {
            background-color: #f2f2f2;
        }
        .label {
            display: inline-block;
            width: 100px;
            font-weight: bold;
            color: #1a5276;
        }
        .message-box {
            border: 1px solid #ddd;
            padding: 15px;
            background-color: #f9f9f9;
            border-radius: 4px;
            margin-top: 10px;
        }
        .footer {
            position: absolute;
            bottom: 0;
            width: 100%;
            background-color: #1a5276;
            color: white;
            text-align: center;
            padding: 15px 0;
            font-size: 12px;
        }
        .page-number {
            position: absolute;
            bottom: 15px;
            right: 40px;
            font-size: 12px;
        }
        .no-response {
            font-style: italic;
            color: #777;
            padding: 15px;
        }
        .watermark {
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%) rotate(-45deg);
            font-size: 100px;
            color: rgba(26, 82, 118, 0.05);
            z-index: -1;
            font-weight: bold;
        }
    </style>
</head>
<body>
    <div class="page">
        <div class="watermark">SwapCircle</div>
        
        <div class="header">
            <div class="logo-area">
                <div class="logo-text">SwapCircle</div>
            </div>
            <div class="document-title">Reclamation Document</div>
        </div>

        <div class="content">
            <div class="section">
                <h3>Reclamation Information</h3>
                <div class="info-row">
                    <span class="label">Title:</span> {{ reclamation.titre }}
                </div>
                <div class="info-row">
                    <span class="label">Type:</span> {{ typeReclamation }}
                </div>
                <div class="info-row">
                    <span class="label">Status:</span> {{ reclamation.statut }}
                </div>
                <div class="info-row">
                    <span class="label">Date:</span> {{ reclamation.dateReclamation|date('Y-m-d H:i') }}
                </div>
                <div class="info-row">
                    <span class="label">Message:</span>
                    <div class="message-box">{{ reclamation.message }}</div>
                </div>
            </div>

            <div class="section">
                <h3>User Information</h3>
                <div class="info-row">
                    <span class="label">Name:</span> {{ utilisateur.nom }} {{ utilisateur.prenom }}
                </div>
                <div class="info-row">
                    <span class="label">Email:</span> {{ utilisateur.email }}
                </div>
                <div class="info-row">
                    <span class="label">Role:</span> {{ utilisateur.role }}
                </div>
            </div>

            <div class="section">
                <h3>Response Information</h3>
                {% if reponse %}
                    <div class="info-row">
                        <span class="label">Response:</span>
                        <div class="message-box">{{ reponse.contenu }}</div>
                    </div>
                    <div class="info-row">
                        <span class="label">Date:</span> {{ reponse.dateReponse|date('Y-m-d H:i') }}
                    </div>
                {% else %}
                    <div class="no-response">No response yet.</div>
                {% endif %}
            </div>
        </div>

        <div class="footer">
            SwapCircle © {{ "now"|date('Y') }} | This is an official document | Contact: support@swapcircle.com
        </div>
        
        <div class="page-number">Page 1 of 1</div>
    </div>
</body>
</html>